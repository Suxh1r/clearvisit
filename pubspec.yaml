name: carecue
description: CareCue, a private, local-first medical visit organizer.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  cryptography: ^2.9.0
  file_picker: ^13.1.0
  flutter:
    sdk: flutter
  flutter_local_notifications: ^19.0.0
  flutter_secure_storage: ^10.3.1
  flutter_timezone: ^4.0.0
  path: ^1.9.1
  path_provider: ^2.1.6
  sqflite_sqlcipher: ^3.4.0
  timezone: ^0.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

